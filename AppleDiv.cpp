#include <iostream>
#include <vector>
#include <unordered_map>
#include <algorithm>
#include <string>

int main()
{
	int n;
	std::cin >> n;
	inputv
	std::sort(v.begin(), v.end());
	long long sum = 0;
	for (int num : v)
		sum += num;
	return 0;
}
// runtime beats %
// memory beats %
